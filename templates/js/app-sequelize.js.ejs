const express = require('express');
const path = require('path');
const { query } = require('./query');
const app = express();

// 监听端口
app.listen(8082, () => {
  console.log('listen port 8082 success');
});

app.get('/', function (req, res) {
  query();
  res.send('Hello World')
});